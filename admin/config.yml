backend:
  name: github
  repo: NopWebGit/unife-portal
  branch: main

media_folder: "uploads"
public_folder: "/uploads"

collections:
  - name: "contenuti"
    label: "Contenuti"
    folder: "content"
    create: true
    slug: "{{slug}}"
    fields:
      - { label: "Titolo", name: "title", widget: "string" }
      - { label: "Testo", name: "body", widget: "markdown" }
      - { label: "Immagine di copertina", name: "image", widget: "image", required: false }
      - label: "Tag"
        name: "tags"
        widget: "list"
        required: false
        hint: "Aggiungi uno o più tag separati da invio"
      - { label: "Link esterno", name: "link", widget: "string", required: false }
      - { label: "Etichetta pulsante", name: "button_label", widget: "string", required: false }
